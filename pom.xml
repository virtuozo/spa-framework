<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>hitz</groupId>
  <artifactId>virtuozo</artifactId>
  <version>1.0.0</version>
  <name>Hitz Virtuozo</name>
  <packaging>pom</packaging>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <java.compiler.version>1.6</java.compiler.version>
    <gwt.version>2.6.1</gwt.version>
  </properties>

  <modules>
    <module>api</module>
    <module>bootstrap</module>
    <module>rest-api</module>
    <module>widgets</module>
    
    <module>commons</module>
  </modules>
  
  <dependencyManagement>
    <dependencies>
      <!-- Shared dependencies -->
      <dependency>
        <groupId>javax.validation</groupId>
        <artifactId>validation-api</artifactId>
        <version>1.0.0.GA</version>
      </dependency>
      <dependency>
        <groupId>log4j</groupId>
        <artifactId>log4j</artifactId>
        <version>1.2.17</version>
      </dependency>
      
            <!-- GWT -->
      <dependency>
        <groupId>com.google.gwt</groupId>
        <artifactId>gwt-user</artifactId>
        <version>${gwt.version}</version>
        <scope>provided</scope>
      </dependency>
      
      <dependency>
        <groupId>org.fusesource.restygwt</groupId>
        <artifactId>restygwt</artifactId>
        <version>1.3.1</version>
        <scope>provided</scope>
      </dependency>

      <!-- virtuozo modules -->
      <dependency>
        <groupId>hitz</groupId>
        <artifactId>virtuozo-api</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>hitz</groupId>
        <artifactId>virtuozo-bootstrap</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>hitz</groupId>
        <artifactId>virtuozo-rest-api</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>hitz</groupId>
        <artifactId>virtuozo-widgets</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>hitz</groupId>
        <artifactId>virtuozo-commons</artifactId>
        <version>${project.version}</version>
      </dependency>

      <!-- Testing -->
      <dependency>
        <groupId>junit</groupId>
        <artifactId>junit</artifactId>
        <version>4.7</version>
        <scope>test</scope>
      </dependency>
      <dependency>
        <groupId>org.easymock</groupId>
        <artifactId>easymock</artifactId>
        <version>3.2</version>
        <scope>test</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>2.1</version>
        </plugin>
        <plugin>
          <groupId>net.alchim31.maven</groupId>
          <artifactId>yuicompressor-maven-plugin</artifactId>
          <version>1.1</version>
        </plugin>
      </plugins>
    </pluginManagement>

    <!--Add sources to jar files -->
    <resources>
      <resource>
        <directory>src/main/java</directory>
      </resource>
      <resource>
        <directory>src/main/resources</directory>
      </resource>
    </resources>

    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <source>${java.compiler.version}</source>
          <target>${java.compiler.version}</target>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <licenses>
    <license>
      <name>The Apache Software License, Version 2.0</name>
      <url>http://www.apache.org/licenses/LICENSE-2.0.txt</url>
      <distribution>repo</distribution>
    </license>
  </licenses>

  <distributionManagement>
    <repository>
      <id>deployment</id>
      <url>${deployment.releases.url}</url>
    </repository>

    <snapshotRepository>
      <id>deployment</id>
      <url>${deployment.snaphosts.url}</url>
    </snapshotRepository>
  </distributionManagement>

  <issueManagement>
    <system>Github issue tracker</system>
    <url>https://github.com/brazoft/virtuozo/issues</url>
  </issueManagement>
  
  <scm>
    <connection>scm:git:git://github.com/brazoft/virtuozo.git</connection>
    <developerConnection>scm:git:git@github.com:/brazoft/virtuozo.git</developerConnection>
    <url>http://github.com/brazoft/virtuozo</url>
  </scm>
  
  <developers>
    <developer>
      <id>braz</id>
      <name>Anderson Braz</name>
      <email>anderson.braz@gmail.com</email>
    </developer>
  </developers>

  <organization>
    <name>Brazoft</name>
  </organization>
  <url>http://hitz-project.uni.me</url>
  <description>Virtuozo is a spa framework on top of GWT and Bootstrap.</description>
</project>
